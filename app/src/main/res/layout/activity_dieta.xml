<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:fab="http://schemas.android.com/apk/res-auto"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.prjvt.intellifitn.fragments.TreinoFragment">

    <android.support.v7.widget.Toolbar
        android:id="@+id/tb_dieta"
        android:layout_height="?attr/actionBarSize"
        android:layout_width="match_parent"
        android:minHeight="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:layout_alignParentLeft="true"
        app:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"/>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rv_dieta"
        android:layout_below="@id/tb_dieta"
        android:scrollbars="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <TextView
        android:id="@+id/tv_hint_dieta"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/hint_activity_dieta"
        android:textSize="18sp"
        android:gravity="center"
        android:layout_margin="30dp"
        android:alpha="0.6"
        android:layout_centerInParent="true"/>

    <!-- Botao flutuante !-->
    <com.software.shell.fab.ActionButton
        android:id="@+id/bt_float_dieta"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        fab:button_color="@color/colorPrimary"
        fab:button_colorPressed="@color/colorPrimaryDark"
        fab:image="@drawable/fab_plus_icon"
        fab:show_animation="@anim/fab_roll_from_down"
        />

</RelativeLayout>
